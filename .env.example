# Neon Postgres connection string. Use the -pooler endpoint.
DATABASE_URL=

# Signs session cookies. Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Origin Better Auth builds callback URLs from.
BETTER_AUTH_URL=http://localhost:3000
